# Day12 RTP-Gate / RTD Results

This directory contains the current lab-root RTD snapshot.

Included:

- `rtd_scores/`: per-candidate RTD JSON summaries and item-level CSV files.
- `traces/gsm8k_dense_trace_manifest.json`: trace manifest with counts and trace configuration.

Excluded:

- dense trace JSONL files
- raw generation/classification JSONL streams
- model snapshots and caches
- logs and virtual environments

The raw downstream evaluation was still running when this snapshot was exported. Add a later results commit once GSM8K, XSUM, classification, and saved-model consistency checks are complete.

